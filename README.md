# aabdi-static-pages-deploy

A minimal Vue + Vite project deployed using GitHub Pages and GitHub Actions.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

The site deploys automatically on pushes to `main`.
